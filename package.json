{
  "name": "larpress-docs",
  "version": "1.0.0",
  "description": "larpress Documentation",
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "docs:deploy:zip": "npm run docs:build && cd docs/.vitepress/dist && zip -r ../../../docs-dist.zip . && cd ../../.. && echo '✅ Build complete! Upload docs-dist.zip to your server and extract it to your document root.'",
    "docs:deploy:info": "echo '📦 Build output location: docs/.vitepress/dist/' && echo '📤 Upload all files from this directory to your web server document root'"
  },
  "devDependencies": {
    "vitepress": "^1.0.0",
    "vue": "^3.4.0"
  }
}

